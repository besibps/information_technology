import 'package:flutter/material.dart';

class InputOfWorkOrder extends StatelessWidget {
  const InputOfWorkOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Input Of Work Order"));
  }
}
