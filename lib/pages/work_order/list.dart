import 'package:flutter/material.dart';

class ListOfWorkOrder extends StatelessWidget {
  const ListOfWorkOrder({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("List Of Work Order"));
  }
}
